# Your AWS account ID (12-digit number)
AWS_ACCOUNT=123456789012

# Region to deploy into — must match AWS_REGION in apps/server/.env
AWS_REGION=eu-west-1

# AWS CLI profile to use for deploy/diff/synth
AWS_PROFILE=default
